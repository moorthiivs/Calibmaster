import { Card } from "react-rainbow-components";
import "./Invoices.css";

const Invoices = (props) => {
  return (
    <Card className="basic__card">
      <p>Invoices Content</p>
    </Card>
  );
};

export default Invoices;
